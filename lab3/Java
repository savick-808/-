1. Бинарная куча

Реализация алгоритма:

-bubbleUp(int idx): поднимает элемент вверх по дереву, пока родитель меньше текущего элемента. Это обеспечивает свойство max-кучи (родитель всегда больше детей)
-sinkDown(int idx): опускает элемент вниз, выбирая наибольшего ребёнка среди возможных кандидатов и меняя их местами, чтобы восстановить структуру кучи
-insert(int element): добавляет новый элемент в конец массива и вызывает метод bubbleUp, чтобы переместить его на правильную позицию
-extractMax(): извлекает максимальное значение (корень), заменяя его последним элементом и вызывая sinkDown, чтобы сохранить свойства кучи
-swap(int a, int b): меняет местами два элемента массива.

2. Биноминальная куча 

-merge(BinomialHeap other): объединяет две кучи путём добавления всех деревьев второй кучи в первую и последующего устранения дублирования рангов
-addTree(BinomialTreeNode<T> node): вставляет новое дерево в кучу, проверяя наличие конфликтующих рангов и соединяя одинаковые ранги
-link(BinomialTreeNode<T> first, BinomialTreeNode<T> second): присоединяет одно дерево к другому, сохраняя иерархическое правило (меньший элемент становится родителем большего)
-insert(T item): создаёт одиночное дерево и вставляет его в кучу
-findMin(): находит наименьший элемент среди корней деревьев, возвращая его значение

3. Куча фибоначи

-concatenateLists(FibonacciHeapNode<K> list1, FibonacciHeapNode<K> list2): конструирует циклический список путём соединения двух списков
-removeFromList(FibonacciHeapNode<K> node): удаляет указанный узел из списка
-cascadeCut(FibonacciHeapNode<K> node): рекурсивно обрезает связи узлов после извлечения минимума
-updateMinRoot(): обновление корня минимальной вершины после изменения структуры кучи
-extractMin(): возвращает и удаляет минимальное значение из кучи, применяя каскадное сокращение связей

4. Хеш-таблица

-put(K key, V value): сохраняет пару ключ-значение в таблице, определяя индекс через хэш-функцию.
-get(K key): ищет значение по указанному ключу, проходя по соответствующей корзине.
-hashFunction(K key): реализует простую хэш-функцию, приводящую хэш-код ключа к диапазону размеров таблицы.
