1.Бинарная куча
Бинарная куча - дерево, в котором каждый узел имеет не более двух потомков, и значение родительского узла больше (max-heap) или меньше (min-heap) значений дочерних узлов

Реализация бинарной кучи:
import heapq 

numbers = [1, 2, 3, 4]  
heapq.heapify(numbers) 
heapq.heappush(numbers, 0) 
min_value = heapq.heappop(numbers) 
print(numbers) 

heapq модуль - готовый min-heap

2.Биномиальная куча 

Биномиальная куча - коллекция биномиальных деревьев, где каждое дерево удовлетворяет свойству кучи и имеет строго определённую структуру

Реализация в Python:
 
class BinomialNode:
    def __init__(self, key):
        self.key = key
        self.degree = 0
        self.child = None    # Простые ссылки
        self.sibling = None  # Сборщик мусора управляет памятью

Реализуется вручную: классы BinomialNode + BinomialHeap с полями key, degree, child, sibling

3.Куча Фибоначчи (Fibonacci Heap)

Куча Фибоначчи - коллекция деревьев, удовлетворяющих свойству min-heap, с ленивым объединением и эффективными амортизированными операциями

Реализация в Python:

class FibonacciNode:
    def __init__(self, key):
        self.key = key
        self.degree = 0
        self.marked = False
        self.parent = self.child = None
        self.left = self.right = self 

Реализуется вручную: класс FibonacciHeap с циклическим двусвязным списком корней

4. Хеш-таблица

Хеш-таблица - структура данных, в которой все элементы хранятся в виде пары «ключ-значение», где ключ — уникальное число для индексации, а значение — связанные с ним данные

Рализация на Python:

1)HashTable инициализирует таблицу фиксированного размера (size) и создает массив списков ("bucket")
2)Метод hash_function() преобразует ключ в индекс массива, используя встроенную функцию hash()
3)Метод insert() добавляет элемент либо обновляет значение, если ключ уже существует
4)Метод search() ищет значение по заданному ключу

